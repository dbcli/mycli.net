Title: mycli
Slug: index

Mycli is a command line interface for MySQL, MariaDB, Percona, and TiDB with:

* auto-completion
* syntax highlighting
* fuzzy history search
* LLM support
* and much more!

</br>

# Source

[https://github.com/dbcli/mycli](https://github.com/dbcli/mycli)

</br>

# Bug tracker

[https://github.com/dbcli/mycli/issues](https://github.com/dbcli/mycli/issues)


<figure>
<img src="/images/main.png" width=700 align="center" data-alt="/images/main.gif">
</figure>

</br>

# Quick Start

If you already know how to install Python packages, then you can simply do:

    :::bash
    $ pip install mycli[all]

If you're on macOS you can install an up-to-date mycli via Homebrew:

    :::bash
    $ brew update && brew install mycli

You may also want to install [less] and [fzf] for pager support and fuzzy history
search.  `less` is usually installed by default on Mac and Linux systems.

If you're having trouble with the quick start, check the [install] page for
detailed instructions.

</br>

# Usage

```text
$ mycli --help
Usage: mycli [OPTIONS] [DATABASE]

  A MySQL terminal client with auto-completion and syntax highlighting.

  Examples:
    - mycli my_database
    - mycli -u my_user -h my_host.com my_database
    - mycli mysql://my_user@my_host.com:3306/my_database

Options:
  -h, --host TEXT                 Host address of the database.
  -P, --port INTEGER              Port number to use for connection. Honors
                                  $MYSQL_TCP_PORT.
  -u, --user TEXT                 User name to connect to the database.
  -S, --socket TEXT               The socket file to use for connection.
  -p, --pass, --password TEXT     Prompt for (or enter in cleartext) password
                                  to connect to the database.
  --ssl-mode [auto|on|off]        Set desired SSL behavior. auto=preferred,
                                  on=required, off=off.
  --ssl / --no-ssl                Enable SSL for connection (automatically
                                  enabled with other flags).
  --ssl-ca PATH                   CA file in PEM format.
  --ssl-capath TEXT               CA directory.
  --ssl-cert PATH                 X509 cert in PEM format.
  --ssl-key PATH                  X509 key in PEM format.
  --ssl-cipher TEXT               SSL cipher to use.
  --tls-version [tlsv1|tlsv1.1|tlsv1.2|tlsv1.3]
                                  TLS protocol version for secure connection.
  --ssl-verify-server-cert        Verify server's "Common Name" in its cert
                                  against hostname used when connecting. This
                                  option is disabled by default.
  -V, --version                   Output mycli's version.
  -v, --verbose                   Verbose output.
  -D, --database TEXT             Database to use.
  -d, --dsn TEXT                  Use DSN configured into the [alias_dsn]
                                  section of myclirc file.
  --list-dsn                      list of DSN configured into the [alias_dsn]
                                  section of myclirc file.
  -R, --prompt TEXT               Prompt format (Default: "\t \u@\h:\d> ").
  -l, --logfile FILENAME          Log every query and its results to a file.
  --checkpoint FILENAME           In batch or --execute mode, log successful
                                  queries to a file.
  --defaults-group-suffix TEXT    Read MySQL config groups with the specified
                                  suffix.
  --defaults-file PATH            Only read MySQL options from the given file.
  --myclirc PATH                  Location of myclirc file.
  --auto-vertical-output          Automatically switch to vertical output mode
                                  if the result is wider than the terminal
                                  width.
  --show-warnings / --no-show-warnings
                                  Automatically show warnings after executing
                                  a SQL statement.
  -t, --table                     Shorthand for --format=table.
  --csv                           Shorthand for --format=csv.
  --warn / --no-warn              Warn before running a destructive query.
  --local-infile BOOLEAN          Enable/disable LOAD DATA LOCAL INFILE.
  -g, --login-path TEXT           Read this path from the login file.
  -e, --execute TEXT              Execute command and quit.
  --init-command TEXT             SQL statement to execute after connecting.
  --unbuffered                    Instead of copying every row of data into a
                                  buffer, fetch rows as needed, to save
                                  memory.
  --charset TEXT                  Character set for MySQL session.
  --password-file PATH            File or FIFO path containing the password to
                                  connect to the db if not specified
                                  otherwise.
  --noninteractive                Don't prompt during batch input.
                                  Recommended.
  --format [default|csv|tsv|table]
                                  Format for batch or --execute output.
  --throttle FLOAT                Pause in seconds between queries in batch
                                  mode.
  --use-keyring [true|false|reset]
                                  Store and retrieve passwords from the system
                                  keyring: true/false/reset.
  --checkup                       Run a checkup on your config file.
  --help                          Show this message and exit.
```

See also the [`help` guide]({filename}/pages/commands.md) at the mycli prompt,
and the [`~/.myclirc`]({filename}/pages/config.md) file for configuration
which persists across sessions.

[install]: {filename}/pages/1.install.md
[less]: https://github.com/gwsw/less
[fzf]: https://github.com/junegunn/fzf
